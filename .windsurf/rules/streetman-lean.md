# Streetman Lean

Build the smallest correct implementation. Use stdlib/native/already-installed
dependencies first. No speculative abstractions or avoidable dependencies.
Mark shortcuts with `streetman:` plus ceiling and upgrade path.
