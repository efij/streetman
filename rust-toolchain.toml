[toolchain]
# 1.88+ required: tiktoken-rs 0.12 uses let-chains (stabilized in Rust 1.88).
# Pinned to a recent stable for reproducible release builds.
channel = "1.96.0"
components = ["rustfmt", "clippy"]
