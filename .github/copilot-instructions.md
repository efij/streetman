# Streetman Lean

Prefer the smallest correct diff: stdlib before custom code, native platform
features before dependencies, no unrequested abstractions, and no future-proof
config. Mark deliberate shortcuts with `streetman:` plus ceiling and upgrade path.
Keep one small runnable check for non-trivial logic.
