# Streetman Lean

Use the Streetman Lean ladder: skip unnecessary work, prefer stdlib, prefer
native platform features, use installed dependencies, then one-line/minimum code.
No unrequested abstractions or new deps. Keep one runnable check for non-trivial logic.
