description = "Audit the repo for avoidable code and dependency bloat"
prompt = "Run Streetman Lean audit across the repo. Rank what can be deleted, replaced with stdlib/native behavior, or inlined until a second implementation exists."
